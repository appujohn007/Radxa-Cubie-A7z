diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
index ff3e8702..577d58fb 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
@@ -1012,13 +1012,17 @@ static void rwnx_rx_add_rtap_hdr(struct rwnx_hw* rwnx_hw,
         aggregation = rxvect->ht.aggregation;
         *pos = 0;
     } else {
-        struct ieee80211_supported_band* band =
-                rwnx_hw->wiphy->bands[phy_info->phy_band];
+        struct ieee80211_supported_band* band = NULL;
+        if (phy_info->phy_band < NUM_NL80211_BANDS)
+            band = rwnx_hw->wiphy->bands[phy_info->phy_band];
         rtap->it_present |= cpu_to_le32(1 << IEEE80211_RADIOTAP_RATE);
         BUG_ON((rate_idx = legrates_lut[rxvect->leg_rate].idx) == -1);
         if (phy_info->phy_band == NL80211_BAND_5GHZ)
             rate_idx -= 4;  /* rwnx_ratetable_5ghz[0].hw_value == 4 */
-        *pos = DIV_ROUND_UP(band->bitrates[rate_idx].bitrate, 5);
+        if (band && band->bitrates && rate_idx >= 0 && rate_idx < band->n_bitrates)
+            *pos = DIV_ROUND_UP(band->bitrates[rate_idx].bitrate, 5);
+        else
+            *pos = 2; /* fallback 1 Mbps */
     }
     pos++;
 
@@ -2071,12 +2075,14 @@ u8 rwnx_rxdataind_aicwf(struct rwnx_hw *rwnx_hw, void *hostid, void *rx_priv)
     hw_rxhdr = (struct hw_rxhdr *)skb->data;
 
 #ifdef AICWF_RX_REORDER
-    if(hw_rxhdr->is_monitor_vif) {
+    if (hw_rxhdr->is_monitor_vif ||
+        (rwnx_hw->monitor_vif != RWNX_INVALID_VIF &&
+         (hw_rxhdr->flags_vif_idx == rwnx_hw->monitor_vif ||
+          hw_rxhdr->flags_vif_idx == RWNX_INVALID_VIF))) {
         status = RX_STAT_MONITOR;
         //printk("monitor rx\n");
-    }
+    } else
 #endif
-
     if(hw_rxhdr->flags_upload)
         status |= RX_STAT_FORWARD;
 
@@ -2100,9 +2106,9 @@ u8 rwnx_rxdataind_aicwf(struct rwnx_hw *rwnx_hw, void *hostid, void *rx_priv)
 
         //Check if monitor interface exists and is open
         rwnx_vif = rwnx_rx_get_vif(rwnx_hw, rwnx_hw->monitor_vif);
-        if (!rwnx_vif) {
-            dev_err(rwnx_hw->dev, "Received monitor frame but there is no monitor interface open, skb->len:%u\n", skb->len);
-            goto check_len_update;
+        if (!rwnx_vif || !rwnx_vif->up) {
+            dev_kfree_skb(skb);
+            goto end;
         }
 
         rwnx_rx_vector_convert(rwnx_hw,
@@ -2149,6 +2155,11 @@ u8 rwnx_rxdataind_aicwf(struct rwnx_hw *rwnx_hw, void *hostid, void *rx_priv)
         #endif
         }
 
+        if (!skb_monitor) {
+            dev_kfree_skb(skb);
+            goto end;
+        }
+
         skb_reset_tail_pointer(skb);
         skb->len = 0;
         skb_reset_tail_pointer(skb_monitor);
@@ -2422,6 +2433,11 @@ check_len_update:
                 goto end;
             }
 
+            if (rwnx_vif->wdev.iftype == NL80211_IFTYPE_MONITOR) {
+                dev_kfree_skb(skb);
+                goto end;
+            }
+
             if (hw_rxhdr->flags_sta_idx != RWNX_INVALID_STA) {
                 struct rwnx_sta *sta;
 
