diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/Makefile b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/Makefile
index d0970cb..5d8753b 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/Makefile
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/Makefile
@@ -143,7 +143,8 @@ CONFIG_RWNX_RADAR ?= y
 CONFIG_RWNX_BCMC ?= y
 
 # Enable Monitor+Data interface support (need FW support)
-CONFIG_RWNX_MON_DATA =n
+CONFIG_RWNX_MON_DATA = y
+
 CONFIG_RWNX_MON_XMIT ?= n
 CONFIG_RWNX_MON_RXFILTER ?= n
 
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_txrxif.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_txrxif.c
index b848421..aaafccb 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_txrxif.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_txrxif.c
@@ -266,24 +266,59 @@ void aicwf_bus_deinit(struct device *dev)
 void aicwf_frame_tx(void *dev, struct sk_buff *skb)
 {
 #ifdef AICWF_SDIO_SUPPORT
-    struct aic_sdio_dev *sdiodev = (struct aic_sdio_dev *)dev;
+    struct aic_sdio_dev *sdiodev;
+#else
+    struct aic_usb_dev *usbdev;
+    struct rwnx_txhdr *txhdr = NULL;
+    unsigned long flags;
+#endif
+
+    if (!dev) {
+        printk(KERN_ERR "MONDBG: [frame_tx] ERROR: dev is NULL!\n");
+        if (skb) dev_kfree_skb_any(skb);
+        return;
+    }
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [frame_tx] ERROR: skb is NULL!\n");
+        return;
+    }
+
+#ifdef AICWF_SDIO_SUPPORT
+    sdiodev = (struct aic_sdio_dev *)dev;
+    if (!sdiodev->bus_if) {
+        printk(KERN_ERR "MONDBG: [frame_tx] ERROR: sdiodev->bus_if is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return;
+    }
     aicwf_bus_txdata(sdiodev->bus_if, skb);
 #else
-    struct aic_usb_dev *usbdev = (struct aic_usb_dev *)dev;
-	struct rwnx_txhdr *txhdr = NULL;
-	unsigned long flags;
+    usbdev = (struct aic_usb_dev *)dev;
+
+    printk(KERN_ERR "MONDBG: [frame_tx] entered: usbdev=%p, usbdev->state=%d, skb=%p, skb->len=%u\n",
+           usbdev, usbdev->state, skb, skb->len);
 
     if (!usbdev->state) {
+        printk(KERN_ERR "MONDBG: [frame_tx] ERROR: usbdev->state is DOWN (0)!\n");
         txrx_err("down\n");
-		spin_lock_irqsave(&usbdev->tx_flow_lock, flags);
-		if(!usbdev->tbusy)
-			aicwf_usb_tx_flowctrl(usbdev->rwnx_hw, true);
-		spin_unlock_irqrestore(&usbdev->tx_flow_lock, flags);
-		txhdr = (struct rwnx_txhdr *)skb->data;
-		kmem_cache_free(usbdev->rwnx_hw->sw_txhdr_cache, txhdr->sw_hdr);
+        spin_lock_irqsave(&usbdev->tx_flow_lock, flags);
+        if(!usbdev->tbusy)
+            aicwf_usb_tx_flowctrl(usbdev->rwnx_hw, true);
+        spin_unlock_irqrestore(&usbdev->tx_flow_lock, flags);
+        txhdr = (struct rwnx_txhdr *)skb->data;
+        if (txhdr && txhdr->sw_hdr && usbdev->rwnx_hw && usbdev->rwnx_hw->sw_txhdr_cache) {
+            kmem_cache_free(usbdev->rwnx_hw->sw_txhdr_cache, txhdr->sw_hdr);
+        }
         dev_kfree_skb(skb);
         return;
     }
+
+    if (!usbdev->bus_if) {
+        printk(KERN_ERR "MONDBG: [frame_tx] ERROR: usbdev->bus_if is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [frame_tx] calling aicwf_bus_txdata, bus_if=%p\n", usbdev->bus_if);
     aicwf_bus_txdata(usbdev->bus_if, skb);
 #endif
 }
@@ -380,6 +415,8 @@ int aicwf_tasklet_rxframes(struct aicwf_rx_priv *rx_priv)
 	u8 *data = NULL;
 	u8_l *msg = NULL;
 
+    printk(KERN_ERR "MONDBG: [tasklet_rxframes] entered: rx_priv=%p\n", rx_priv);
+
 	while (1) {
 		//aicwf_usb_rx_submit_all_urb_(rx_priv->usbdev);
 		spin_lock_irqsave(&rx_priv->rxqlock, flags);
@@ -837,6 +874,7 @@ int aicwf_process_rxframes(struct aicwf_rx_priv *rx_priv)
 #endif
 
             if((skb->data[2] & USB_TYPE_CFG) != USB_TYPE_CFG) { // type : data
+                printk(KERN_ERR "MONDBG: [process_rxframes] dispatching DATA skb=%p\n", skb);
                 rwnx_rxdataind_aicwf(rx_priv->usbdev->rwnx_hw, skb, (void *)rx_priv);
             }
             else { //  type : config
@@ -854,14 +892,20 @@ int aicwf_process_rxframes(struct aicwf_rx_priv *rx_priv)
                 }
                 memcpy(msg, data, aggr_len + 4);
 
-                if(((*(msg + 2) & 0x7f) == USB_TYPE_CFG_CMD_RSP) && (rx_priv->usbdev->bus_if->state != (int)USB_DOWN_ST))
+                if(((*(msg + 2) & 0x7f) == USB_TYPE_CFG_CMD_RSP) && (rx_priv->usbdev->bus_if->state != (int)USB_DOWN_ST)) {
+                    printk(KERN_ERR "MONDBG: [process_rxframes] dispatching CMD_RSP msg=%p\n", msg + 4);
                     rwnx_rx_handle_msg(rx_priv->usbdev->rwnx_hw, (struct ipc_e2a_msg *)(msg + 4));
+                }
 
-                if((*(msg + 2) & 0x7f) == USB_TYPE_CFG_DATA_CFM)
+                if((*(msg + 2) & 0x7f) == USB_TYPE_CFG_DATA_CFM) {
+                    printk(KERN_ERR "MONDBG: [process_rxframes] dispatching DATA_CFM msg=%p\n", msg + 4);
                     aicwf_usb_host_tx_cfm_handler(&(rx_priv->usbdev->rwnx_hw->usb_env), (u32 *)(msg + 4));
+                }
 
-                if ((*(msg + 2) & 0x7f) == USB_TYPE_CFG_PRINT)
+                if ((*(msg + 2) & 0x7f) == USB_TYPE_CFG_PRINT) {
+                    printk(KERN_ERR "MONDBG: [process_rxframes] dispatching PRINT msg=%p\n", msg + 4);
                     rwnx_rx_handle_print(rx_priv->usbdev->rwnx_hw, msg + 4, aggr_len);
+                }
 
                 skb_pull(skb, adjust_len+4);
                 kfree(msg);
@@ -872,6 +916,7 @@ int aicwf_process_rxframes(struct aicwf_rx_priv *rx_priv)
     }
 #endif
 
+    printk(KERN_ERR "MONDBG: [process_rxframes] exit ret=%d\n", ret);
     return ret;
 #endif //AICWF_SDIO_SUPPORT
 }
@@ -1141,9 +1186,11 @@ bool aicwf_rxframe_enqueue(struct device *dev, struct frame_queue *q, struct sk_
 
 void aicwf_dev_skb_free(struct sk_buff *skb)
 {
-    if (!skb)
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [dev_skb_free] skb is NULL\n");
         return;
-
+    }
+    printk(KERN_ERR "MONDBG: [dev_skb_free] freeing skb=%p, len=%u\n", skb, skb->len);
     dev_kfree_skb_any(skb);
 }
 
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_usb.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_usb.c
index c086988..1d5cf3a 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_usb.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/aicwf_usb.c
@@ -176,17 +176,37 @@ void rwnx_wake_sta_all_queues(struct rwnx_sta *sta, struct rwnx_hw *rwnx_hw)
 
 static void usb_txc_sta_flowctrl(struct aicwf_usb_buf *usb_buf, struct aic_usb_dev *usb_dev)
 {
+    printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] entered: usb_buf=%p, usb_dev=%p\n", usb_buf, usb_dev);
 #ifdef CONFIG_PER_STA_FC
     unsigned long flags;
 	struct rwnx_sta *sta;
 	struct txdesc_api *hostdesc;
 	u8 sta_idx;
+
+    if (!usb_buf) {
+        printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] ERROR: usb_buf is NULL!\n");
+        return;
+    }
+    if (!usb_buf->skb) {
+        printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] ERROR: usb_buf->skb is NULL!\n");
+        return;
+    }
+
 	if(usb_buf->cfm)
 		hostdesc = (struct txdesc_api *)((u8 *)usb_buf->skb + 4);
 	else
 		hostdesc = (struct txdesc_api *)((u8 *)usb_buf->skb->data + 4);
+
+    if (!hostdesc) {
+        printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] ERROR: hostdesc is NULL!\n");
+        return;
+    }
+
 	//printk("txcpl: sta %d\n", hostdesc->host.staid);
 	sta_idx = hostdesc->host.staid;
+    printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] sta_idx=%u, flags=0x%x, cfm=%d\n",
+           sta_idx, hostdesc->host.flags, usb_buf->cfm);
+
 	if(sta_idx < NX_REMOTE_STA_MAX && !(hostdesc->host.flags & TXU_CNTRL_MGMT)) {
 		struct rwnx_vif *vif = NULL;
 		sta = &usb_dev->rwnx_hw->sta_table[sta_idx];
@@ -194,7 +214,7 @@ static void usb_txc_sta_flowctrl(struct aicwf_usb_buf *usb_buf, struct aic_usb_d
 		spin_lock_irqsave(&usb_dev->tx_flow_lock, flags);
 		atomic_dec(&usb_dev->rwnx_hw->sta_flowctrl[sta_idx].tx_pending_cnt);
 		//printk("sta:%d, pending:%d, flowctrl=%d\n", sta->sta_idx, sta->tx_pending_cnt, sta->flowctrl);
-		if(RWNX_VIF_TYPE(vif) == NL80211_IFTYPE_AP) {
+		if(vif && RWNX_VIF_TYPE(vif) == NL80211_IFTYPE_AP) {
 			if(atomic_read(&usb_dev->rwnx_hw->sta_flowctrl[sta_idx].tx_pending_cnt) < AICWF_USB_FC_PERSTA_LOW_WATER &&
 							usb_dev->rwnx_hw->sta_flowctrl[sta_idx].flowctrl) {
 				//AICWFDBG(LOGDEBUG, "sta 0x%x:0x%x, %d pending %d, wake\n", sta->mac_addr[4], sta->mac_addr[5], sta->sta_idx, atomic_read(&usb_dev->rwnx_hw->sta_flowctrl[sta_idx].tx_pending_cnt));
@@ -206,17 +226,38 @@ static void usb_txc_sta_flowctrl(struct aicwf_usb_buf *usb_buf, struct aic_usb_d
 		spin_unlock_irqrestore(&usb_dev->tx_flow_lock, flags);
 	}
 #endif
+    printk(KERN_ERR "MONDBG: [usb_txc_sta_flowctrl] exit\n");
 }
 
 static void aicwf_usb_tx_complete(struct urb *urb)
 {
     unsigned long flags;
-    struct aicwf_usb_buf *usb_buf = (struct aicwf_usb_buf *) urb->context;
-    struct aic_usb_dev *usb_dev = usb_buf->usbdev;
+    struct aicwf_usb_buf *usb_buf;
+    struct aic_usb_dev *usb_dev;
     #ifndef CONFIG_USB_TX_AGGR
     struct sk_buff *skb;
     #endif
 
+    if (!urb) {
+        printk(KERN_ERR "MONDBG: [usb_tx_complete] ERROR: urb is NULL!\n");
+        return;
+    }
+
+    usb_buf = (struct aicwf_usb_buf *) urb->context;
+    if (!usb_buf) {
+        printk(KERN_ERR "MONDBG: [usb_tx_complete] ERROR: urb->context (usb_buf) is NULL!\n");
+        return;
+    }
+
+    usb_dev = usb_buf->usbdev;
+    if (!usb_dev) {
+        printk(KERN_ERR "MONDBG: [usb_tx_complete] ERROR: usb_buf->usbdev is NULL!\n");
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [usb_tx_complete] URB complete: urb=%p, status=%d, actual_len=%u, cfm=%d, skb=%p\n",
+           urb, urb->status, urb->actual_length, usb_buf->cfm, usb_buf->skb);
+
 	usb_txc_sta_flowctrl(usb_buf, usb_dev);
 
 #ifdef CONFIG_USB_ALIGN_DATA
@@ -228,13 +269,17 @@ static void aicwf_usb_tx_complete(struct urb *urb)
 #ifndef CONFIG_USB_TX_AGGR
     if (usb_buf->cfm == false) {
         skb = usb_buf->skb;
-        dev_kfree_skb_any(skb);
+        printk(KERN_ERR "MONDBG: [usb_tx_complete] cfm==false: freeing non-cfm skb=%p via dev_kfree_skb_any\n", skb);
+        if (skb)
+            dev_kfree_skb_any(skb);
     }
     #if !defined CONFIG_USB_NO_TRANS_DMA_MAP
     else {
         u8 *buf;
         buf = (u8 *)usb_buf->skb;
-        kfree(buf);
+        printk(KERN_ERR "MONDBG: [usb_tx_complete] cfm==true: freeing buf=%p via kfree\n", buf);
+        if (buf)
+            kfree(buf);
     }
     #endif
     usb_buf->skb = NULL;
@@ -242,6 +287,7 @@ static void aicwf_usb_tx_complete(struct urb *urb)
     AICWFDBG(LOGDEBUG,"tx com %d\n", usb_buf->aggr_cnt);
     usb_buf->aggr_cnt = 0;
 #endif//CONFIG_USB_TX_AGGR
+    printk(KERN_ERR "MONDBG: [usb_tx_complete] returning usb_buf=%p to tx_free_list\n", usb_buf);
     aicwf_usb_tx_queue(usb_dev, &usb_dev->tx_free_list, usb_buf,
                     &usb_dev->tx_free_count, &usb_dev->tx_free_lock);
 
@@ -249,10 +295,12 @@ static void aicwf_usb_tx_complete(struct urb *urb)
     if (usb_dev->tx_free_count > AICWF_USB_TX_HIGH_WATER) {
         if (usb_dev->tbusy) {
             usb_dev->tbusy = false;
+            printk(KERN_ERR "MONDBG: [usb_tx_complete] waking tx_flowctrl, tbusy set to false\n");
             aicwf_usb_tx_flowctrl(usb_dev->rwnx_hw, false);
         }
     }
     spin_unlock_irqrestore(&usb_dev->tx_flow_lock, flags);
+    printk(KERN_ERR "MONDBG: [usb_tx_complete] exit\n");
 }
 
 void aicwf_usb_rx_submit_all_urb_(struct aic_usb_dev *usb_dev);
@@ -261,11 +309,19 @@ void aicwf_usb_rx_submit_all_urb_(struct aic_usb_dev *usb_dev);
 static void aicwf_usb_rx_complete(struct urb *urb)
 {
     struct aicwf_usb_buf *usb_buf = (struct aicwf_usb_buf *) urb->context;
-    struct aic_usb_dev *usb_dev = usb_buf->usbdev;
-    struct aicwf_rx_priv* rx_priv = usb_dev->rx_priv;
+    struct aic_usb_dev *usb_dev = usb_buf ? usb_buf->usbdev : NULL;
+    struct aicwf_rx_priv* rx_priv = usb_dev ? usb_dev->rx_priv : NULL;
     struct rx_buff *rx_buff = NULL;
     unsigned long flags = 0;
 
+    printk(KERN_ERR "MONDBG: [usb_rx_complete] (PREALLOC) entered: urb=%p, status=%d, actual_len=%u, usb_buf=%p, usb_dev=%p\n",
+           urb, urb ? urb->status : -1, urb ? urb->actual_length : 0, usb_buf, usb_dev);
+
+    if (!usb_buf || !usb_dev || !rx_priv) {
+        printk(KERN_ERR "MONDBG: [usb_rx_complete] ERROR: null context pointers!\n");
+        return;
+    }
+
     rx_buff = usb_buf->rx_buff;
     usb_buf->rx_buff = NULL;
 
@@ -347,12 +403,34 @@ static void aicwf_usb_rx_complete(struct urb *urb)
 #else
 static void aicwf_usb_rx_complete(struct urb *urb)
 {
-    struct aicwf_usb_buf *usb_buf = (struct aicwf_usb_buf *) urb->context;
-    struct aic_usb_dev *usb_dev = usb_buf->usbdev;
-    struct aicwf_rx_priv* rx_priv = usb_dev->rx_priv;
+    struct aicwf_usb_buf *usb_buf;
+    struct aic_usb_dev *usb_dev;
+    struct aicwf_rx_priv* rx_priv;
     struct sk_buff *skb = NULL;
     unsigned long flags = 0;
 
+    if (!urb) {
+        printk(KERN_ERR "MONDBG: [usb_rx_complete] ERROR: urb is NULL!\n");
+        return;
+    }
+
+    usb_buf = (struct aicwf_usb_buf *) urb->context;
+    if (!usb_buf) {
+        printk(KERN_ERR "MONDBG: [usb_rx_complete] ERROR: usb_buf is NULL!\n");
+        return;
+    }
+
+    usb_dev = usb_buf->usbdev;
+    if (!usb_dev) {
+        printk(KERN_ERR "MONDBG: [usb_rx_complete] ERROR: usb_dev is NULL!\n");
+        return;
+    }
+
+    rx_priv = usb_dev->rx_priv;
+
+    printk(KERN_ERR "MONDBG: [usb_rx_complete] entered: urb=%p, status=%d, actual_len=%u, skb=%p, state=%d\n",
+           urb, urb->status, urb->actual_length, usb_buf->skb, usb_dev->state);
+
     skb = usb_buf->skb;
     usb_buf->skb = NULL;
 
@@ -1170,6 +1248,13 @@ static void aicwf_usb_tx_process(struct aic_usb_dev *usb_dev)
     int ret = 0;
     u8* data = NULL;
 
+    if (!usb_dev) {
+        printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: usb_dev is NULL!\n");
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [usb_tx_process] entered: usb_dev=%p, state=%d\n", usb_dev, usb_dev->state);
+
 #ifdef CONFIG_USB_TX_AGGR
     if (!aicwf_is_framequeue_empty(&usb_dev->tx_priv->txq)) {
         if (aicwf_usb_send(usb_dev->tx_priv)) {
@@ -1181,21 +1266,34 @@ static void aicwf_usb_tx_process(struct aic_usb_dev *usb_dev)
     while(!list_empty(&usb_dev->tx_post_list)) {
 
         if (usb_dev->state != USB_UP_ST) {
-            usb_err("usb state is not up!\n");
+            printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: usb state is not up (%d)!\n", usb_dev->state);
             return;
         }
 
         usb_buf = aicwf_usb_tx_dequeue(usb_dev, &usb_dev->tx_post_list,
                         &usb_dev->tx_post_count, &usb_dev->tx_post_lock);
         if(!usb_buf) {
-            usb_err("can not get usb_buf from tx_post_list!\n");
+            printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: can not get usb_buf from tx_post_list!\n");
             return;
         }
-        data = usb_buf->skb->data;
+
+        if (!usb_buf->urb) {
+            printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: usb_buf->urb is NULL!\n");
+            goto fail;
+        }
+
+        if (!usb_dev->udev) {
+            printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: usb_dev->udev is NULL!\n");
+            goto fail;
+        }
+
+        data = usb_buf->skb ? usb_buf->skb->data : NULL;
+        printk(KERN_ERR "MONDBG: [usb_tx_process] submitting USB URB=%p, pipe=0x%x, data=%p, len=%u, cfm=%d\n",
+               usb_buf->urb, usb_dev->bulk_out_pipe, data, usb_buf->urb->transfer_buffer_length, usb_buf->cfm);
 
         ret = usb_submit_urb(usb_buf->urb, GFP_KERNEL);
         if (ret) {
-            AICWFDBG(LOGERROR, "aicwf_usb_bus_tx usb_submit_urb FAILED err:%d\n", ret);
+            printk(KERN_ERR "MONDBG: [usb_tx_process] ERROR: usb_submit_urb FAILED ret=%d\n", ret);
             #ifdef CONFIG_USB_TX_AGGR
             aicwf_usb_tx_queue(usb_dev, &usb_dev->tx_post_list, usb_buf,
                     &usb_dev->tx_post_count, &usb_dev->tx_post_lock);
@@ -1203,6 +1301,8 @@ static void aicwf_usb_tx_process(struct aic_usb_dev *usb_dev)
             #else
             goto fail;
             #endif
+        } else {
+            printk(KERN_ERR "MONDBG: [usb_tx_process] usb_submit_urb SUCCESS\n");
         }
 
         continue;
@@ -1723,10 +1823,10 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
     struct aicwf_usb_buf *usb_buf;
     int ret = 0;
     unsigned long flags;
-    struct aicwf_bus *bus_if = dev_get_drvdata(dev);
-    struct aic_usb_dev *usb_dev = bus_if->bus_priv.usb;
-    struct rwnx_txhdr *txhdr = (struct rwnx_txhdr *)skb->data;
-    struct rwnx_hw *rwnx_hw = usb_dev->rwnx_hw;
+    struct aicwf_bus *bus_if;
+    struct aic_usb_dev *usb_dev;
+    struct rwnx_txhdr *txhdr;
+    struct rwnx_hw *rwnx_hw;
     u8 usb_header[4];
     u8 adj_buf[4] = {0};
     u16 index = 0;
@@ -1736,8 +1836,55 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
     int align;
 #endif
 
+    if (!dev) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: dev is NULL!\n");
+        if (skb) dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: skb is NULL!\n");
+        return -EINVAL;
+    }
+
+    bus_if = dev_get_drvdata(dev);
+    if (!bus_if) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: bus_if (dev_get_drvdata) is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+
+    usb_dev = bus_if->bus_priv.usb;
+    if (!usb_dev) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: usb_dev is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+
+    if (!skb->data) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: skb->data is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+
+    txhdr = (struct rwnx_txhdr *)skb->data;
+    if (!txhdr || !txhdr->sw_hdr) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: txhdr or txhdr->sw_hdr is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+
+    rwnx_hw = usb_dev->rwnx_hw;
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: rwnx_hw is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return -EINVAL;
+    }
+
+    printk(KERN_ERR "MONDBG: [usb_bus_txdata] STEP 1: entered: skb=%p, skb_len=%u, usb_state=%d, need_cfm=%d, raw_frame=%d\n",
+           skb, skb->len, usb_dev->state, txhdr->sw_hdr->need_cfm, txhdr->sw_hdr->raw_frame);
+
     if (usb_dev->state != USB_UP_ST) {
-        usb_err("usb state is not up!\n");
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: usb state (%d) != USB_UP_ST!\n", usb_dev->state);
         kmem_cache_free(rwnx_hw->sw_txhdr_cache, txhdr->sw_hdr);
         dev_kfree_skb_any(skb);
         return -EIO;
@@ -1746,13 +1893,16 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
     usb_buf = aicwf_usb_tx_dequeue(usb_dev, &usb_dev->tx_free_list,
                         &usb_dev->tx_free_count, &usb_dev->tx_free_lock);
     if (!usb_buf) {
-        usb_err("free:%d, post:%d\n", usb_dev->tx_free_count, usb_dev->tx_post_count);
+        printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: aicwf_usb_tx_dequeue returned NULL usb_buf (free_count=%d, post_count=%d)!\n",
+               usb_dev->tx_free_count, usb_dev->tx_post_count);
         kmem_cache_free(rwnx_hw->sw_txhdr_cache, txhdr->sw_hdr);
         dev_kfree_skb_any(skb);
         ret = -ENOMEM;
         goto flow_ctrl;
     }
 
+    printk(KERN_ERR "MONDBG: [usb_bus_txdata] STEP 2: dequeued usb_buf=%p, urb=%p\n", usb_buf, usb_buf->urb);
+
     usb_tx_flow_ctrl(txhdr, usb_dev, rwnx_hw);
 
     if (txhdr->sw_hdr->need_cfm) {
@@ -1762,6 +1912,13 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
         #else
         buf = kmalloc(skb->len + 1, GFP_ATOMIC/*GFP_KERNEL*/);
         #endif
+        if (!buf) {
+            printk(KERN_ERR "MONDBG: [usb_bus_txdata] ERROR: kmalloc for buf failed!\n");
+            kmem_cache_free(rwnx_hw->sw_txhdr_cache, txhdr->sw_hdr);
+            dev_kfree_skb_any(skb);
+            ret = -ENOMEM;
+            goto flow_ctrl;
+        }
         index += sizeof(usb_header);
         memcpy(&buf[index], (u8 *)(long)&txhdr->sw_hdr->desc, sizeof(struct txdesc_api));
         index += sizeof(struct txdesc_api);
@@ -1811,8 +1968,8 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
 
 #ifndef CONFIG_USE_USB_ZERO_PACKET
 	if((buf_len % 512) == 0){
-		printk("%s send zero package buf_len: %d\r\n", __func__, buf_len);
-		if(txhdr->sw_hdr->need_cfm){
+		printk(KERN_ERR "MONDBG: [usb_bus_txdata] send zero package buf_len: %d\n", buf_len);
+		if(need_cfm){
 			buf[buf_len] = 0x00;
 			buf_len = buf_len + 1;
 		}else{
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_main.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_main.c
index 0ea1687..caef1ba 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_main.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_main.c
@@ -1663,8 +1663,42 @@ static struct net_device_stats *rwnx_get_stats(struct net_device *dev)
 u16 rwnx_select_queue(struct net_device *dev, struct sk_buff *skb,
                       struct net_device *sb_dev)
 {
-    struct rwnx_vif *rwnx_vif = netdev_priv(dev);
-    return rwnx_select_txq(rwnx_vif, skb);
+    struct rwnx_vif *rwnx_vif;
+    u16 q_idx;
+
+    if (!dev) {
+        printk(KERN_ERR "MONDBG: [select_queue] dev is NULL!\n");
+        return 0;
+    }
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [select_queue] skb is NULL!\n");
+        return 0;
+    }
+
+    rwnx_vif = netdev_priv(dev);
+    if (!rwnx_vif) {
+        printk(KERN_ERR "MONDBG: [select_queue] rwnx_vif (netdev_priv) is NULL on dev %s (ifindex %d)!\n",
+               dev->name, dev->ifindex);
+        return 0;
+    }
+
+    if (!rwnx_vif->rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [select_queue] rwnx_vif->rwnx_hw is NULL on dev %s (vif_idx %d, iftype %d)!\n",
+               dev->name, rwnx_vif->vif_index, rwnx_vif->wdev.iftype);
+        return 0;
+    }
+
+    if (rwnx_vif->wdev.iftype == NL80211_IFTYPE_MONITOR) {
+        printk(KERN_ERR "MONDBG: [select_queue] MONITOR xmit select_queue on dev %s (vif %p, skb len %u, prio %u)\n",
+               dev->name, rwnx_vif, skb->len, skb->priority);
+    }
+
+    q_idx = rwnx_select_txq(rwnx_vif, skb);
+    if (rwnx_vif->wdev.iftype == NL80211_IFTYPE_MONITOR) {
+        printk(KERN_ERR "MONDBG: [select_queue] MONITOR select_queue returned queue_idx=%u\n", q_idx);
+    }
+
+    return q_idx;
 }
 
 /**
@@ -2282,9 +2316,14 @@ static int rwnx_cfg80211_change_iface(struct wiphy *wiphy,
        (RWNX_VIF_TYPE(vif) != NL80211_IFTYPE_MONITOR)) {
         struct rwnx_vif *vif_el;
         list_for_each_entry(vif_el, &rwnx_hw->vifs, list) {
-            // Check if data interface already exists
+            /*
+             * Monitor mode is allowed only with no active data VIFs. Ignore
+             * the monitor VIF itself and the dedicated P2P_DEVICE VIF, which
+             * is not a data interface and is treated like MONITOR here.
+             */
             if ((vif_el != vif) &&
-               (RWNX_VIF_TYPE(vif) != NL80211_IFTYPE_MONITOR)) {
+               (RWNX_VIF_TYPE(vif_el) != NL80211_IFTYPE_MONITOR) &&
+               (RWNX_VIF_TYPE(vif_el) != NL80211_IFTYPE_P2P_DEVICE)) {
                 wiphy_err(rwnx_hw->wiphy,
                         "Monitor+Data interface support (MON_DATA) disabled\n");
                 return -EIO;
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_msg_rx.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_msg_rx.c
index e640fd0..c48e8d2 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_msg_rx.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_msg_rx.c
@@ -1606,14 +1606,22 @@ static msg_cb_fct *msg_hdlrs[] = {
  */
 void rwnx_rx_handle_msg(struct rwnx_hw *rwnx_hw, struct ipc_e2a_msg *msg)
 {
-	//printk("%s(%d) MSG_T(msg->id):%d MSG_I(msg->id):%d cmd:%s\r\n", __func__, 
-	//	msg->id,
-	//	MSG_T(msg->id), 
-	//	MSG_I(msg->id),
-	//	rwnx_id2str[MSG_T(msg->id)][MSG_I(msg->id)]);
-	
+    if (!msg) {
+        printk(KERN_ERR "MONDBG: [rx_handle_msg] ERROR: msg is NULL!\n");
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [rx_handle_msg] entered: msg_id=0x%x (T:%d, I:%d), rwnx_hw=%p\n",
+           msg->id, MSG_T(msg->id), MSG_I(msg->id), rwnx_hw);
+
+    if (!rwnx_hw || !rwnx_hw->cmd_mgr) {
+        printk(KERN_ERR "MONDBG: [rx_handle_msg] ERROR: rwnx_hw or cmd_mgr is NULL!\n");
+        return;
+    }
+
     rwnx_hw->cmd_mgr->msgind(rwnx_hw->cmd_mgr, msg,
                             msg_hdlrs[MSG_T(msg->id)][MSG_I(msg->id)]);
+    printk(KERN_ERR "MONDBG: [rx_handle_msg] exit\n");
 }
 
 void rwnx_rx_handle_print(struct rwnx_hw *rwnx_hw, u8 *msg, u32 len)
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
index ff3e870..070467e 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_rx.c
@@ -1233,9 +1233,18 @@ static int rwnx_rx_monitor(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif,
                            struct sk_buff *skb,  struct hw_rxhdr *hw_rxhdr_ptr,
                            u8 rtap_len)
 {
+    printk(KERN_ERR "MONDBG: [rx_monitor] entered: rwnx_hw=%p, rwnx_vif=%p, skb=%p, rtap_len=%u\n",
+           rwnx_hw, rwnx_vif, skb, rtap_len);
+
+    if (!rwnx_vif || !rwnx_vif->ndev || !skb) {
+        printk(KERN_ERR "MONDBG: [rx_monitor] ERROR: null pointer (rwnx_vif=%p, skb=%p)!\n", rwnx_vif, skb);
+        return -1;
+    }
+
     skb->dev = rwnx_vif->ndev;
 
     if (rwnx_vif->wdev.iftype != NL80211_IFTYPE_MONITOR) {
+        printk(KERN_ERR "MONDBG: [rx_monitor] ERROR: not a monitor vif (iftype=%d)\n", rwnx_vif->wdev.iftype);
         netdev_err(rwnx_vif->ndev, "not a monitor vif\n");
         return -1;
     }
@@ -1254,9 +1263,11 @@ static int rwnx_rx_monitor(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif,
     filter_rx_tcp_ack(rwnx_hw, skb->data, cpu_to_le16(skb->len));
 #endif
 
+    printk(KERN_ERR "MONDBG: [rx_monitor] passing skb=%p (len=%u) to netif_receive_skb\n", skb, skb->len);
     local_bh_disable();
     netif_receive_skb(skb);
     local_bh_enable();
+    printk(KERN_ERR "MONDBG: [rx_monitor] netif_receive_skb complete\n");
 
     return 0;
 }
@@ -2068,6 +2079,15 @@ u8 rwnx_rxdataind_aicwf(struct rwnx_hw *rwnx_hw, void *hostid, void *rx_priv)
     const struct ethhdr *eth;
 
     REG_SW_SET_PROFILING(rwnx_hw, SW_PROF_RWNXDATAIND);
+
+    printk(KERN_ERR "MONDBG: [rxdataind_aicwf] entered: rwnx_hw=%p, hostid=%p, rx_priv=%p\n",
+           rwnx_hw, hostid, rx_priv);
+
+    if (!skb || !skb->data) {
+        printk(KERN_ERR "MONDBG: [rxdataind_aicwf] ERROR: skb or skb->data is NULL!\n");
+        return 0;
+    }
+
     hw_rxhdr = (struct hw_rxhdr *)skb->data;
 
 #ifdef AICWF_RX_REORDER
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_tx.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_tx.c
index d7b9a40..34346b6 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_tx.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_tx.c
@@ -319,6 +319,26 @@ u16 rwnx_select_txq(struct rwnx_vif *rwnx_vif, struct sk_buff *skb)
             sta = rwnx_vif->sta.ap;
         break;
     }
+    case NL80211_IFTYPE_MONITOR:
+        skb->priority = TID_MGT;
+        if (!rwnx_vif) {
+            printk(KERN_ERR "MONDBG: [select_txq] rwnx_vif is NULL for MONITOR!\n");
+            return 0;
+        }
+        if (!rwnx_vif->rwnx_hw) {
+            printk(KERN_ERR "MONDBG: [select_txq] rwnx_vif->rwnx_hw is NULL for MONITOR!\n");
+            return 0;
+        }
+        txq = rwnx_txq_vif_get(rwnx_vif, NX_UNK_TXQ_TYPE);
+        if (!txq) {
+            printk(KERN_ERR "MONDBG: [select_txq] rwnx_txq_vif_get returned NULL txq for MONITOR (vif_idx %d)!\n",
+                   rwnx_vif->vif_index);
+            return 0;
+        }
+        netdev_queue = txq->ndev_idx;
+        printk(KERN_ERR "MONDBG: [select_txq] MONITOR select_txq: txq=%p, txq->idx=%d, ndev_idx=%u\n",
+               txq, txq->idx, netdev_queue);
+        return netdev_queue;
     case NL80211_IFTYPE_AP_VLAN:
         if (rwnx_vif->ap_vlan.sta_4a) {
             sta = rwnx_vif->ap_vlan.sta_4a;
@@ -573,15 +593,51 @@ static int rwnx_prep_tx(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr)
  */
 void rwnx_tx_push(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr, int flags)
 {
-    struct rwnx_sw_txhdr *sw_txhdr = txhdr->sw_hdr;
-    struct sk_buff *skb = sw_txhdr->skb;
-    struct rwnx_txq *txq = sw_txhdr->txq;
-    u16 hw_queue = txq->hwq->id;
+    struct rwnx_sw_txhdr *sw_txhdr;
+    struct sk_buff *skb;
+    struct rwnx_txq *txq;
+    u16 hw_queue;
     int user = 0;
 
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: rwnx_hw is NULL!\n");
+        return;
+    }
+    if (!txhdr) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: txhdr is NULL!\n");
+        return;
+    }
+
+    sw_txhdr = txhdr->sw_hdr;
+    if (!sw_txhdr) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: sw_txhdr is NULL!\n");
+        return;
+    }
+
+    skb = sw_txhdr->skb;
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: skb is NULL!\n");
+        return;
+    }
+
+    txq = sw_txhdr->txq;
+    if (!txq) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: txq is NULL!\n");
+        return;
+    }
+
+    if (!txq->hwq) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: txq->hwq is NULL!\n");
+        return;
+    }
+
+    hw_queue = txq->hwq->id;
+
     lockdep_assert_held(&rwnx_hw->tx_lock);
 
-    //printk("rwnx_tx_push\n");
+    printk(KERN_ERR "MONDBG: [tx_push] entered: txhdr=%p, sw_txhdr=%p, skb=%p, skb->len=%u, hw_queue=%u, raw_frame=%d, fixed_rate=%d, sta=%p, vif=%p\n",
+           txhdr, sw_txhdr, skb, skb->len, hw_queue, sw_txhdr->raw_frame, sw_txhdr->fixed_rate, sw_txhdr->rwnx_sta, sw_txhdr->rwnx_vif);
+
     /* RETRY flag is not always set so retest here */
     if (txq->nb_retry) {
         flags |= RWNX_PUSH_RETRY;
@@ -663,19 +719,27 @@ void rwnx_tx_push(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr, int flags)
             sw_txhdr->desc.host.status_desc_addr = 0;
         }
 
-        sw_txhdr->rwnx_vif->net_stats.tx_packets++;
-        sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+        if (sw_txhdr->rwnx_vif) {
+            sw_txhdr->rwnx_vif->net_stats.tx_packets++;
+            sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+        }
         rwnx_hw->stats.last_tx = jiffies;
     }
     aicwf_frame_tx((void *)(rwnx_hw->sdiodev), skb);
 #endif
 #ifdef AICWF_USB_SUPPORT
+    if (!rwnx_hw->usbdev) {
+        printk(KERN_ERR "MONDBG: [tx_push] ERROR: rwnx_hw->usbdev is NULL!\n");
+        return;
+    }
+
     if( ((sw_txhdr->desc.host.flags & TXU_CNTRL_MGMT) && \
 	((*(skb->data+sw_txhdr->headroom)==0xd0) || (*(skb->data+sw_txhdr->headroom)==0x10) || (*(skb->data+sw_txhdr->headroom)==0x30))) || \
         (sw_txhdr->desc.host.ethertype == 0x8e88) || (sw_txhdr->desc.host.ethertype == 0xb488)) {
 	// 0xd0:Action, 0x10:AssocRsp, 0x8e88:EAPOL, 0xb488: WAPI
         sw_txhdr->need_cfm = 1;
         sw_txhdr->desc.host.status_desc_addr = ((1<<31) | rwnx_hw->usb_env.txdesc_free_idx[0]);
+        printk(KERN_ERR "MONDBG: [tx_push] USB need_cfm=1, status_desc_addr=0x%x\n", sw_txhdr->desc.host.status_desc_addr);
         aicwf_usb_host_txdesc_push(&(rwnx_hw->usb_env), 0, (long)(skb));
         if((sw_txhdr->desc.host.flags & TXU_CNTRL_MGMT))
 		AICWFDBG(LOGINFO, "need cfm mgmt:%x,user_idx=%d, skb=%p sta_idx:%d\n",
@@ -700,10 +764,18 @@ void rwnx_tx_push(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr, int flags)
             sw_txhdr->desc.host.status_desc_addr = 0;
         }
 
-        sw_txhdr->rwnx_vif->net_stats.tx_packets++;
-        sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+        printk(KERN_ERR "MONDBG: [tx_push] USB need_cfm=0, flags=0x%x, status_desc_addr=0x%x, rate_config=0x%x\n",
+               sw_txhdr->desc.host.flags, sw_txhdr->desc.host.status_desc_addr, sw_txhdr->rate_config);
+
+        if (sw_txhdr->rwnx_vif) {
+            sw_txhdr->rwnx_vif->net_stats.tx_packets++;
+            sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+        } else {
+            printk(KERN_ERR "MONDBG: [tx_push] WARNING: sw_txhdr->rwnx_vif is NULL!\n");
+        }
         rwnx_hw->stats.last_tx = jiffies;
     }
+    printk(KERN_ERR "MONDBG: [tx_push] calling aicwf_frame_tx: usbdev=%p, skb=%p\n", rwnx_hw->usbdev, skb);
     aicwf_frame_tx((void *)(rwnx_hw->usbdev), skb);
 #endif
 #endif
@@ -1856,117 +1928,128 @@ int rwnx_start_mgmt_xmit(struct rwnx_vif *vif, struct rwnx_sta *sta,
 
 netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *dev)
 {
-    int rtap_len, ret, idx, tmp_len;
+    int rtap_len, ret, idx;
     struct ieee80211_radiotap_header *rtap_hdr; // net/ieee80211_radiotap.h
     struct ieee80211_radiotap_iterator iterator; // net/cfg80211.h
-    u8_l *rtap_buf = (u8_l *)skb->data;
+    u8_l *rtap_buf;
     u8_l rate;
 
-    struct rwnx_vif *vif = netdev_priv(dev);
-    struct rwnx_hw *rwnx_hw = vif->rwnx_hw;
+    struct rwnx_vif *vif;
+    struct rwnx_hw *rwnx_hw;
     struct rwnx_txhdr *txhdr;
     struct rwnx_sw_txhdr *sw_txhdr;
     struct txdesc_api *desc;
     struct rwnx_sta *sta;
     struct rwnx_txq *txq;
     u16_l frame_len, headroom, frame_oft;
-    u8_l tid, rate_fmt = FORMATMOD_NON_HT, rate_idx = 0, txsig_bw = PHY_CHNL_BW_20;
+    u8_l rate_fmt = FORMATMOD_NON_HT, rate_idx = 0, txsig_bw = PHY_CHNL_BW_20;
     u8_l *pframe, *data;
     bool robust;
     struct sk_buff *skb_mgmt;
-    bool offchan = false;
-    int nx_off_chan_txq_idx = NX_OFF_CHAN_TXQ_IDX;
 
-    rtap_hdr = (struct ieee80211_radiotap_header*)(rtap_buf);
-    rtap_len = ieee80211_get_radiotap_len(rtap_buf);//max_length
-    frame_len = skb->len;
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: skb is NULL!\n");
+        return NETDEV_TX_OK;
+    }
+    if (!dev) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: dev is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return NETDEV_TX_OK;
+    }
 
-    AICWFDBG(LOGINFO, "rwnx_start_monitor_if_xmit, skb_len=%d, rtap_len=%d\n", skb->len, rtap_len);
-//rwnx_data_dump((char*)__func__, skb->data, skb->len);
-    if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
-        ((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DC ||
-        g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW) && chip_id < 3)){
-            nx_off_chan_txq_idx = NX_OFF_CHAN_TXQ_IDX_FOR_OLD_IC;
+    vif = netdev_priv(dev);
+    if (!vif) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: vif (netdev_priv) is NULL on dev %s!\n", dev->name);
+        dev_kfree_skb_any(skb);
+        return NETDEV_TX_OK;
+    }
+
+    rwnx_hw = vif->rwnx_hw;
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: rwnx_hw is NULL on dev %s (vif_idx %d)!\n", dev->name, vif->vif_index);
+        dev_kfree_skb_any(skb);
+        return NETDEV_TX_OK;
+    }
+
+    rtap_buf = (u8_l *)skb->data;
+    if (!rtap_buf) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: skb->data is NULL!\n");
+        dev_kfree_skb_any(skb);
+        return NETDEV_TX_OK;
     }
 
+    rtap_hdr = (struct ieee80211_radiotap_header*)(rtap_buf);
+    rtap_len = ieee80211_get_radiotap_len(rtap_buf);
+    frame_len = skb->len;
+
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 1: entered xmit on dev %s (ifindex %d, vif %p, vif_idx %d, skb_len %u, rtap_len %d)\n",
+           dev->name, dev->ifindex, vif, vif->vif_index, skb->len, rtap_len);
 
-    if (unlikely(rtap_hdr->it_version)){
-        AICWFDBG(LOGERROR, "%s itv \r\n", __func__);
+    if (unlikely(rtap_hdr->it_version)) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: invalid radiotap version: %u\n", rtap_hdr->it_version);
         goto free_tag;
-        }
+    }
 
-    if (unlikely(skb->len < rtap_len)){
-        AICWFDBG(LOGERROR, "%s skb->len < rtap_len \r\n", __func__);
+    if (unlikely(skb->len < rtap_len)) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: skb->len (%u) < rtap_len (%d)\n", skb->len, rtap_len);
         goto free_tag;
-        }
+    }
 
-    if (unlikely(rtap_len < sizeof(struct ieee80211_radiotap_header))){
-        AICWFDBG(LOGERROR, "%s rtap_len < sizeof(struct ieee80211_radiotap_header) \r\n", __func__);
+    if (unlikely(rtap_len < sizeof(struct ieee80211_radiotap_header))) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: rtap_len (%d) < sizeof radiotap header\n", rtap_len);
         goto free_tag;
-        }
+    }
 
     frame_len -= rtap_len;
     pframe = rtap_buf + rtap_len;
 
-    // Parse radiotap for injection items and overwrite attribs as needed
+    // Parse radiotap for injection items
     ret = ieee80211_radiotap_iterator_init(&iterator, rtap_hdr, rtap_len, NULL);
+    if (ret) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] radiotap iterator init failed ret=%d\n", ret);
+    } else {
+        printk(KERN_ERR "MONDBG: [mon_xmit] STEP 2: parsing radiotap header...\n");
+    }
+
     while (!ret) {
         ret = ieee80211_radiotap_iterator_next(&iterator);
         if (ret) {
             continue;
         }
-        AICWFDBG(LOGDEBUG, "%s iterator.this_arg_index:%d iterator.this_arg:%x\r\n", __func__, 
-            iterator.this_arg_index, *iterator.this_arg);
+        if (unlikely(!iterator.this_arg)) {
+            printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: null radiotap field for index %d\n", iterator.this_arg_index);
+            ret = -EINVAL;
+            goto free_tag;
+        }
         switch (iterator.this_arg_index) {
             case IEEE80211_RADIOTAP_RATE:
-                // This is basic 802.11b/g rate; use MCS/VHT for higher rates
                 rate = *iterator.this_arg;
-                AICWFDBG(LOGDEBUG, "rate=0x%x\n", rate);
                 for (idx = 0; idx < HW_RATE_MAX; idx++) {
                     if ((rate * 5) == tx_legrates_lut_rate[idx]) {
-                        AICWFDBG(LOGDEBUG, "%s datarate:%d \r\n", __func__, tx_legrates_lut_rate[idx]);
                         break;
                     }
                 }
-                
                 if (idx < HW_RATE_MAX) {
                     rate_idx = idx;
-                    AICWFDBG(LOGDEBUG, "rate_idx = %d \r\n", rate_idx);
                 } else {
-                    AICWFDBG(LOGERROR, "invalid radiotap rate: %d\n", rate);
+                    printk(KERN_ERR "MONDBG: [mon_xmit] invalid radiotap rate: %d\n", rate);
                 }
                 break;
 
             case IEEE80211_RADIOTAP_TX_FLAGS: {
                 u16_l txflags = get_unaligned_le16(iterator.this_arg);
-                AICWFDBG(LOGDEBUG, "txflags=0x%x\n", txflags);
-                if ((txflags & IEEE80211_RADIOTAP_F_TX_NOACK) == 0) {
-                    AICWFDBG(LOGDEBUG, "  TX_NOACK\n");
-                }
-                if (txflags & 0x0010) { // Use preconfigured seq num
-                    // NOTE: this is currently ignored due to qos_en=_FALSE and HW seq num override
-                    AICWFDBG(LOGDEBUG, "  GetSequence\n");
-                }
+                (void)txflags;
             }
             break;
 
             case IEEE80211_RADIOTAP_MCS: {
                 u8_l mcs_have = iterator.this_arg[0];
-                AICWFDBG(LOGDEBUG, "mcs_have=0x%x\n", mcs_have);
                 rate_fmt = FORMATMOD_HT_MF;
                 if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_BW) {
                     u8_l bw = (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_BW_MASK);
-                    u8_l ch_offset = 0;
                     if (bw == IEEE80211_RADIOTAP_MCS_BW_40) {
                         txsig_bw = PHY_CHNL_BW_40;
-                    } else if (bw == IEEE80211_RADIOTAP_MCS_BW_20L) {
-                        bw = IEEE80211_RADIOTAP_MCS_BW_20;
-                        ch_offset = 1; // CHNL_OFFSET_LOWER;
-                    } else if (bw == IEEE80211_RADIOTAP_MCS_BW_20U) {
-                        bw = IEEE80211_RADIOTAP_MCS_BW_20;
-                        ch_offset = 2; // CHNL_OFFSET_UPPER;
                     }
-                    AICWFDBG(LOGDEBUG, "  bw=%d, ch_offset=%d\n", bw, ch_offset);
                 }
                 if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_MCS) {
                     u8_l fixed_rate = iterator.this_arg[2] & 0x7f;
@@ -1974,17 +2057,6 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                         fixed_rate = 0;
                     }
                     rate_idx = fixed_rate;
-                    AICWFDBG(LOGDEBUG, "  fixed_rate=0x%x\n", fixed_rate);
-                }
-                if ((mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_GI) && (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_SGI)) {
-                    AICWFDBG(LOGDEBUG, "  sgi\n");
-                }
-                if ((mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_FEC) && (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_FEC_LDPC)) {
-                    AICWFDBG(LOGDEBUG, "  ldpc\n");
-                }
-                if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_STBC) {
-                    u8 stbc = (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_STBC_MASK) >> IEEE80211_RADIOTAP_MCS_STBC_SHIFT;
-                    AICWFDBG(LOGDEBUG, "  stbc=0x%x\n", stbc);
                 }
             }
             break;
@@ -1992,54 +2064,21 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
             case IEEE80211_RADIOTAP_VHT: {
                 unsigned int mcs, nss;
                 u8 known = iterator.this_arg[0];
-                u8 flags = iterator.this_arg[2];
                 rate_fmt = FORMATMOD_VHT;
-                AICWFDBG(LOGDEBUG, "known=0x%x, flags=0x%x\n", known, flags);
-                // NOTE: this code currently only supports 1SS for radiotap defined rates
-                if ((known & IEEE80211_RADIOTAP_VHT_KNOWN_STBC) && (flags & IEEE80211_RADIOTAP_VHT_FLAG_STBC)) {
-                    AICWFDBG(LOGDEBUG, "  stbc\n");
-                }
-                if ((known & IEEE80211_RADIOTAP_VHT_KNOWN_GI) && (flags & IEEE80211_RADIOTAP_VHT_FLAG_SGI)) {
-                    AICWFDBG(LOGDEBUG, "  sgi\n");
-                }
                 if (known & IEEE80211_RADIOTAP_VHT_KNOWN_BANDWIDTH) {
-                    u8_l bw = iterator.this_arg[3] & 0x1F;
-                    AICWFDBG(LOGDEBUG, "  bw=0x%x\n",bw);
-                    // NOTE: there are various L and U, but we just use straight 20/40/80
-                    // since it's not clear how to set CHNL_OFFSET_LOWER/_UPPER with different
-                    // sideband sizes/configurations.  TODO.
-                    // Also, any 160 is treated as 80 due to lack of WIDTH_160.
                     txsig_bw = PHY_CHNL_BW_40;
-                    if (bw == 0) {
-                        txsig_bw = PHY_CHNL_BW_20;
-                        AICWFDBG(LOGDEBUG, "  20M\n");
-                    } else if (bw >=1 && bw <= 3) {
-                        AICWFDBG(LOGDEBUG, "  40M\n");
-                    } else if (bw >=4 && bw <= 10) {
-                        AICWFDBG(LOGDEBUG, "  80M\n");
-                    } else if (bw >= 11 && bw <= 25) {
-                        AICWFDBG(LOGDEBUG, "  160M\n");
-                    }
                 }
-                // User 0
-                nss = iterator.this_arg[4] & 0x0F; // Number of spatial streams
-                AICWFDBG(LOGINFO, "  nss=0x%x\n", nss);
+                nss = iterator.this_arg[4] & 0x0F;
                 if (nss > 0) {
-                    if (nss > 4) nss = 4;
-                    mcs = (iterator.this_arg[4]>>4) & 0x0F; // MCS rate index
+                    mcs = (iterator.this_arg[4]>>4) & 0x0F;
                     if (mcs > 8) mcs = 9;
                     rate_idx = mcs;
-                    AICWFDBG(LOGDEBUG, "    mcs=0x%x\n", mcs);
-                    if (iterator.this_arg[8] & IEEE80211_RADIOTAP_CODING_LDPC_USER0) {
-                        AICWFDBG(LOGDEBUG, "    ldpc\n");
-                    }
                 }
             }
             break;
 
             case IEEE80211_RADIOTAP_HE: {
                 u16 data1 = ((u16)iterator.this_arg[1] << 8) | iterator.this_arg[0];
-                u16 data2 = ((u16)iterator.this_arg[3] << 8) | iterator.this_arg[2];
                 u16 data3 = ((u16)iterator.this_arg[5] << 8) | iterator.this_arg[4];
                 u16 data5 = ((u16)iterator.this_arg[9] << 8) | iterator.this_arg[8];
                 u8 fmt_he = data1 & IEEE80211_RADIOTAP_HE_DATA1_FORMAT_MASK;
@@ -2059,79 +2098,66 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                     u8 bw = data5 & IEEE80211_RADIOTAP_HE_DATA5_DATA_BW_RU_ALLOC;
                     txsig_bw = (bw == IEEE80211_RADIOTAP_HE_DATA5_DATA_BW_RU_ALLOC_20MHZ) ? PHY_CHNL_BW_20 : PHY_CHNL_BW_40;
                 }
-                if (data2 & IEEE80211_RADIOTAP_HE_DATA2_GI_KNOWN) {
-                    u8 gi = (data5 & IEEE80211_RADIOTAP_HE_DATA5_GI) >> 4;
-                    AICWFDBG(LOGDEBUG, "  gi: %d\n", gi);
-                }
             }
             break;
 
             default:
-                AICWFDBG(LOGERROR, "unparsed arg: 0x%x\n",iterator.this_arg_index);
                 break;
         }
     }
 
-    #if 0
-    // dump buffer
-    tmp_len = 128;
-    if (skb->len < 128) {
-        tmp_len = skb->len;
-    }
-    for (idx = 0; idx < tmp_len; idx+=16) {
-        printk("[%04X] %02X %02X %02X %02X %02X %02X %02X %02X   %02X %02X %02X %02X %02X %02X %02X %02X\n", idx,
-            rtap_buf[idx+0],rtap_buf[idx+1],rtap_buf[idx+2],rtap_buf[idx+3],
-            rtap_buf[idx+4],rtap_buf[idx+5],rtap_buf[idx+6],rtap_buf[idx+7],
-            rtap_buf[idx+8],rtap_buf[idx+9],rtap_buf[idx+10],rtap_buf[idx+11],
-            rtap_buf[idx+12],rtap_buf[idx+13],rtap_buf[idx+14],rtap_buf[idx+15]);
-    }
-    #endif
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 3: radiotap parsed: frame_len=%u, rate_fmt=%u, rate_idx=%u, txsig_bw=%u\n",
+           frame_len, rate_fmt, rate_idx, txsig_bw);
 
-    /* Get the STA id and TID information */
-    sta = rwnx_get_tx_priv(vif, skb, &tid);
-    //if (!sta) {
-    //    printk("sta=null, tid=0x%x\n", tid);
-    //}
-    /* Set TID and Queues indexes */
-    if (sta) {
-        txq = rwnx_txq_sta_get(sta, 8, rwnx_hw);
-    } else {
-        if (offchan)
-            txq = &rwnx_hw->txq[nx_off_chan_txq_idx];
-        else
-            txq = rwnx_txq_vif_get(vif, NX_UNK_TXQ_TYPE);
+    sta = NULL;
+    txq = rwnx_txq_vif_get(vif, NX_UNK_TXQ_TYPE);
+    if (!txq) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: rwnx_txq_vif_get returned NULL txq!\n");
+        goto free_tag;
     }
+
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 4: selected TXQ=%p, txq->idx=%d, txq->status=0x%x, txq->hwq=%p\n",
+           txq, txq->idx, txq->status, txq->hwq);
+
     if (txq->idx == TXQ_INACTIVE) {
-        AICWFDBG(LOGERROR, "TXQ_INACTIVE\n");
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: txq->idx is TXQ_INACTIVE!\n");
         goto free_tag;
     }
+
+    if (!txq->hwq) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: txq->hwq is NULL!\n");
+        goto free_tag;
+    }
+
     // prepare to xmit
     headroom = sizeof(struct rwnx_txhdr);
     skb_mgmt = dev_alloc_skb(headroom + frame_len);
     if (!skb_mgmt) {
-        AICWFDBG(LOGERROR, "skb_mgmt alloc fail\n");
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: skb_mgmt dev_alloc_skb failed!\n");
         goto free_tag;
     }
     skb_reserve(skb_mgmt, headroom);
     data = skb_put(skb_mgmt, frame_len);
     /* Copy the provided data */
     memcpy(data, pframe, frame_len);
-    robust = ieee80211_is_robust_mgmt_frame(
-#if (LINUX_VERSION_CODE < KERNEL_VERSION(3, 15, 0))
-		(void*)skb_mgmt
-#else
-		skb_mgmt
-#endif
-		);
+    robust = ieee80211_is_robust_mgmt_frame(skb_mgmt);
     skb_push(skb_mgmt, headroom);
+
     /* Fill the TX Header */
     txhdr = (struct rwnx_txhdr *)skb_mgmt->data;
     txhdr->hw_hdr.cfm.status.value = 0;
+
+    if (!rwnx_hw->sw_txhdr_cache) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: rwnx_hw->sw_txhdr_cache is NULL!\n");
+        dev_kfree_skb(skb_mgmt);
+        goto free_tag;
+    }
+
     /* Fill the SW TX Header */
     sw_txhdr = kmem_cache_alloc(rwnx_hw->sw_txhdr_cache, GFP_ATOMIC);
     if (unlikely(sw_txhdr == NULL)) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] ERROR: sw_txhdr kmem_cache_alloc failed!\n");
         dev_kfree_skb(skb_mgmt);
-        AICWFDBG(LOGERROR, "sw_txhdr alloc fail\n");
         goto free_tag;
     }
     txhdr->sw_hdr = sw_txhdr;
@@ -2146,7 +2172,8 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
     sw_txhdr->fixed_rate = 1;
     sw_txhdr->rate_config = ((rate_fmt << FORMAT_MOD_TX_RCX_OFT) & FORMAT_MOD_TX_RCX_MASK) |
                             ((txsig_bw << BW_TX_RCX_OFT) & BW_TX_RCX_MASK) |
-                            ((rate_idx << MCS_INDEX_TX_RCX_OFT) & MCS_INDEX_TX_RCX_MASK); // from radiotap
+                            ((rate_idx << MCS_INDEX_TX_RCX_OFT) & MCS_INDEX_TX_RCX_MASK);
+
     /* Fill the Descriptor to be provided to the MAC SW */
     desc = &sw_txhdr->desc;
     desc->host.staid = (sta) ? sta->sta_idx : 0xFF;
@@ -2156,27 +2183,24 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
     if (robust) {
         desc->host.flags |= TXU_CNTRL_MGMT_ROBUST;
     }
-    frame_oft = sizeof(struct rwnx_txhdr) - offsetof(struct rwnx_txhdr, hw_hdr);
-	#if 0
-    #ifdef CONFIG_RWNX_SPLIT_TX_BUF
-    desc->host.packet_addr[0] = sw_txhdr->dma_addr + frame_oft;
-    desc->host.packet_len[0] = frame_len;
-    desc->host.packet_cnt = 1;
-    #else
-    desc->host.packet_addr = sw_txhdr->dma_addr + frame_oft;
-    desc->host.packet_len = frame_len;
-    #endif
-	#else
-	desc->host.packet_len = frame_len;
-	#endif
 
+    desc->host.packet_len = frame_len;
     desc->host.status_desc_addr = sw_txhdr->dma_addr;
 
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 5: skb_mgmt created: skb_mgmt=%p, sw_txhdr=%p, desc=%p, vif_idx=%u, staid=0x%x, rate_cfg=0x%x, status_desc_addr=0x%x\n",
+           skb_mgmt, sw_txhdr, desc, desc->host.vif_idx, desc->host.staid, sw_txhdr->rate_config, desc->host.status_desc_addr);
+
     spin_lock_bh(&rwnx_hw->tx_lock);
-    AICWFDBG(LOGTRACE, "%s send data\r\n", __func__);
-    if (rwnx_txq_queue_skb(skb_mgmt, txq, rwnx_hw, false))
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 6: acquired tx_lock, queueing skb_mgmt to txq %p (hwq %p, id %d)...\n",
+           txq, txq->hwq, txq->hwq->id);
+    if (rwnx_txq_queue_skb(skb_mgmt, txq, rwnx_hw, false)) {
+        printk(KERN_ERR "MONDBG: [mon_xmit] STEP 7: calling rwnx_hwq_process(hwq %p)...\n", txq->hwq);
         rwnx_hwq_process(rwnx_hw, txq->hwq);
+    } else {
+        printk(KERN_ERR "MONDBG: [mon_xmit] rwnx_txq_queue_skb returned 0, not calling hwq_process\n");
+    }
     spin_unlock_bh(&rwnx_hw->tx_lock);
+    printk(KERN_ERR "MONDBG: [mon_xmit] STEP 8: released tx_lock, monitor xmit setup completed successfully\n");
 
 free_tag:
     dev_kfree_skb_any(skb);
@@ -2199,23 +2223,44 @@ int rwnx_txdatacfm(void *pthis, void *host_id)
     struct rwnx_hwq *hwq;
     struct rwnx_txq *txq;
     u16 headroom;
-    //int peek_off = offsetof(struct rwnx_hw_txhdr, cfm);
-    //int peek_len = sizeof(((struct rwnx_hw_txhdr *)0)->cfm);
+
+    printk(KERN_ERR "MONDBG: [txdatacfm] entered: pthis=%p, host_id=%p, skb=%p\n", pthis, host_id, skb);
+
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] ERROR: rwnx_hw is NULL!\n");
+        return -1;
+    }
+    if (!skb || !skb->data) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] ERROR: skb or skb->data is NULL!\n");
+        return -1;
+    }
 
     txhdr = (struct rwnx_txhdr *)skb->data;
+    if (!txhdr) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] ERROR: txhdr is NULL!\n");
+        return -1;
+    }
+
     sw_txhdr = txhdr->sw_hdr;
+    printk(KERN_ERR "MONDBG: [txdatacfm] txhdr=%p, sw_txhdr=%p\n", txhdr, sw_txhdr);
+
+    if (!sw_txhdr) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] ERROR: sw_txhdr is NULL!\n");
+        return -1;
+    }
 
-    /* Read status in the TX control header */
     rwnx_txst = txhdr->hw_hdr.cfm.status;
+    printk(KERN_ERR "MONDBG: [txdatacfm] txst.value=0x%x, vif=%p, sta=%p, flags=0x%x\n",
+           rwnx_txst.value, sw_txhdr->rwnx_vif, sw_txhdr->rwnx_sta, sw_txhdr->desc.host.flags);
 
-    /* Check status in the header. If status is null, it means that the buffer
-     * was not transmitted and we have to return immediately */
     if (rwnx_txst.value == 0) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] txst.value is 0, returning -1\n");
         return -1;
     }
 
 #ifdef AICWF_USB_SUPPORT
-    if (rwnx_hw->usbdev->state == USB_DOWN_ST) {
+    if (rwnx_hw->usbdev && rwnx_hw->usbdev->state == USB_DOWN_ST) {
+        printk(KERN_ERR "MONDBG: [txdatacfm] USB_DOWN_ST: freeing sw_txhdr=%p & skb=%p\n", sw_txhdr, skb);
         headroom = sw_txhdr->headroom;
         kmem_cache_free(rwnx_hw->sw_txhdr_cache, sw_txhdr);
         skb_pull(skb, headroom);
@@ -2234,56 +2279,35 @@ int rwnx_txdatacfm(void *pthis, void *host_id)
 #endif
 
     txq = sw_txhdr->txq;
-    /* don't use txq->hwq as it may have changed between push and confirm */
+    printk(KERN_ERR "MONDBG: [txdatacfm] txq=%p, hw_queue=%u\n", txq, sw_txhdr->hw_queue);
     hwq = &rwnx_hw->hwq[sw_txhdr->hw_queue];
+
+    printk(KERN_ERR "MONDBG: [txdatacfm] calling rwnx_txq_confirm_any\n");
     rwnx_txq_confirm_any(rwnx_hw, txq, hwq, sw_txhdr);
 
-    /* Update txq and HW queue credits */
     if (sw_txhdr->desc.host.flags & TXU_CNTRL_MGMT) {
-        trace_printk("done=%d retry_required=%d sw_retry_required=%d acknowledged=%d\n",
-                     rwnx_txst.tx_done, rwnx_txst.retry_required,
-                     rwnx_txst.sw_retry_required, rwnx_txst.acknowledged);
-#ifdef CREATE_TRACE_POINTS
-        trace_mgmt_cfm(sw_txhdr->rwnx_vif->vif_index,
-                       (sw_txhdr->rwnx_sta) ? sw_txhdr->rwnx_sta->sta_idx : 0xFF,
-                       rwnx_txst.acknowledged);
-#endif
-        /* Confirm transmission to CFG80211 */
-        cfg80211_mgmt_tx_status(&sw_txhdr->rwnx_vif->wdev,
-                                (unsigned long)skb,
-                                (skb->data + sw_txhdr->headroom),
-                                sw_txhdr->frame_len,
-                                rwnx_txst.acknowledged,
-                                GFP_ATOMIC);
-    } else if ((txq->idx != TXQ_INACTIVE) &&
+        printk(KERN_ERR "MONDBG: [txdatacfm] TXU_CNTRL_MGMT flag set, vif=%p\n", sw_txhdr->rwnx_vif);
+        if (sw_txhdr->rwnx_vif) {
+            printk(KERN_ERR "MONDBG: [txdatacfm] calling cfg80211_mgmt_tx_status, wdev=%p\n", &sw_txhdr->rwnx_vif->wdev);
+            cfg80211_mgmt_tx_status(&sw_txhdr->rwnx_vif->wdev,
+                                    (unsigned long)skb,
+                                    (skb->data + sw_txhdr->headroom),
+                                    sw_txhdr->frame_len,
+                                    rwnx_txst.acknowledged,
+                                    GFP_ATOMIC);
+        } else {
+            printk(KERN_ERR "MONDBG: [txdatacfm] ERROR: sw_txhdr->rwnx_vif is NULL during mgmt_tx_status!\n");
+        }
+    } else if (txq && (txq->idx != TXQ_INACTIVE) &&
                (rwnx_txst.retry_required || rwnx_txst.sw_retry_required)) {
         bool sw_retry = (rwnx_txst.sw_retry_required) ? true : false;
-
-        /* Reset the status */
+        printk(KERN_ERR "MONDBG: [txdatacfm] reinjecting retry packet, sw_retry=%d\n", sw_retry);
         txhdr->hw_hdr.cfm.status.value = 0;
-
-        /* The confirmed packet was part of an AMPDU and not acked
-         * correctly, so reinject it in the TX path to be retried */
         rwnx_tx_retry(rwnx_hw, skb, txhdr, sw_retry);
         return 0;
     }
-#ifdef CREATE_TRACE_POINTS
-    trace_skb_confirm(skb, txq, hwq, &txhdr->hw_hdr.cfm);
-#endif
-    /* STA may have disconnect (and txq stopped) when buffers were stored
-       in fw. In this case do nothing when they're returned */
-    if (txq->idx != TXQ_INACTIVE) {
-        #if 0
-        if (txhdr->hw_hdr.cfm.credits) {
-            txq->credits += txhdr->hw_hdr.cfm.credits;
-            if (txq->credits <= 0)
-                rwnx_txq_stop(txq, RWNX_TXQ_STOP_FULL);
-            else if (txq->credits > 0)
-                rwnx_txq_start(txq, RWNX_TXQ_STOP_FULL);
-        }
-        #endif
 
-        /* continue service period */
+    if (txq && txq->idx != TXQ_INACTIVE) {
         if (unlikely(txq->push_limit && !rwnx_txq_is_full(txq))) {
             rwnx_txq_add_to_hw_list(txq);
         }
@@ -2294,14 +2318,18 @@ int rwnx_txdatacfm(void *pthis, void *host_id)
         rwnx_hw->stats.ampdus_tx[txhdr->hw_hdr.cfm.ampdu_size - 1]++;
 
 #ifdef CONFIG_RWNX_AMSDUS_TX
-    txq->amsdu_len = txhdr->hw_hdr.cfm.amsdu_size;
+    if (txq)
+        txq->amsdu_len = txhdr->hw_hdr.cfm.amsdu_size;
 #endif
 
-    /* Update statistics */
-    sw_txhdr->rwnx_vif->net_stats.tx_packets++;
-    sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+    printk(KERN_ERR "MONDBG: [txdatacfm] updating net_stats for vif=%p\n", sw_txhdr->rwnx_vif);
+    if (sw_txhdr->rwnx_vif) {
+        sw_txhdr->rwnx_vif->net_stats.tx_packets++;
+        sw_txhdr->rwnx_vif->net_stats.tx_bytes += sw_txhdr->frame_len;
+    } else {
+        printk(KERN_ERR "MONDBG: [txdatacfm] WARNING: sw_txhdr->rwnx_vif is NULL during net_stats update!\n");
+    }
 
-    /* Release SKBs */
 #ifdef CONFIG_RWNX_AMSDUS_TX
     if (sw_txhdr->desc.host.flags & TXU_CNTRL_AMSDU) {
         struct rwnx_amsdu_txhdr *amsdu_txhdr;
@@ -2313,10 +2341,13 @@ int rwnx_txdatacfm(void *pthis, void *host_id)
 #endif /* CONFIG_RWNX_AMSDUS_TX */
 
     headroom = sw_txhdr->headroom;
+    printk(KERN_ERR "MONDBG: [txdatacfm] freeing sw_txhdr=%p via kmem_cache_free\n", sw_txhdr);
     kmem_cache_free(rwnx_hw->sw_txhdr_cache, sw_txhdr);
     skb_pull(skb, headroom);
+    printk(KERN_ERR "MONDBG: [txdatacfm] consuming skb=%p via consume_skb\n", skb);
     consume_skb(skb);
 
+    printk(KERN_ERR "MONDBG: [txdatacfm] exit success\n");
     return 0;
 }
 
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_txq.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_txq.c
index 5b71597..3650fb2 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_txq.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/rwnx_txq.c
@@ -828,6 +828,22 @@ int rwnx_txq_queue_skb(struct sk_buff *skb, struct rwnx_txq *txq,
     unsigned long flags;
 #endif
 
+    if (!skb) {
+        printk(KERN_ERR "MONDBG: [txq_queue_skb] ERROR: skb is NULL!\n");
+        return 0;
+    }
+    if (!txq) {
+        printk(KERN_ERR "MONDBG: [txq_queue_skb] ERROR: txq is NULL!\n");
+        return 0;
+    }
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [txq_queue_skb] ERROR: rwnx_hw is NULL!\n");
+        return 0;
+    }
+
+    printk(KERN_ERR "MONDBG: [txq_queue_skb] entered: skb=%p, txq=%p (idx %d, status 0x%x), retry=%d\n",
+           skb, txq, txq->idx, txq->status, retry);
+
 #ifdef CONFIG_RWNX_FULLMAC
     if (unlikely(txq->sta && txq->sta->ps.active)) {
         txq->sta->ps.pkt_ready[txq->ps_id]++;
@@ -911,6 +927,15 @@ void rwnx_txq_confirm_any(struct rwnx_hw *rwnx_hw, struct rwnx_txq *txq,
 {
     int user = 0;
 
+    printk(KERN_ERR "MONDBG: [txq_confirm_any] entered: rwnx_hw=%p, txq=%p, hwq=%p, sw_txhdr=%p\n",
+           rwnx_hw, txq, hwq, sw_txhdr);
+
+    if (!txq || !hwq || !rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [txq_confirm_any] ERROR: null pointer (txq=%p, hwq=%p, rwnx_hw=%p)!\n",
+               txq, hwq, rwnx_hw);
+        return;
+    }
+
 #ifdef CONFIG_RWNX_MUMIMO_TX
     int group_id;
 
@@ -929,6 +954,8 @@ void rwnx_txq_confirm_any(struct rwnx_hw *rwnx_hw, struct rwnx_txq *txq,
 
     hwq->need_processing = true;
     rwnx_hw->stats.cfm_balance[hwq->id]--;
+    printk(KERN_ERR "MONDBG: [txq_confirm_any] exit: hwq_id=%d, cfm_balance=%d\n",
+           hwq->id, rwnx_hw->stats.cfm_balance[hwq->id]);
 }
 
 /******************************************************************************
@@ -1236,6 +1263,19 @@ void rwnx_hwq_process(struct rwnx_hw *rwnx_hw, struct rwnx_hwq *hwq)
 #ifndef CONFIG_ONE_TXQ
     unsigned long flags;
 #endif
+
+    if (!rwnx_hw) {
+        printk(KERN_ERR "MONDBG: [hwq_process] ERROR: rwnx_hw is NULL!\n");
+        return;
+    }
+    if (!hwq) {
+        printk(KERN_ERR "MONDBG: [hwq_process] ERROR: hwq is NULL!\n");
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [hwq_process] entered: hwq=%p, hwq_id=%d, hwq_size=%d\n",
+           hwq, hwq->id, hwq->size);
+
 #ifdef CREATE_TRACE_POINTS
     trace_process_hw_queue(hwq);
 #endif
diff --git a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/usb_host.c b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/usb_host.c
index fca8dc4..7f3c57e 100644
--- a/drivers/net/wireless/aic8800/usb/aic8800_fdrv/usb_host.c
+++ b/drivers/net/wireless/aic8800/usb/aic8800_fdrv/usb_host.c
@@ -77,55 +77,58 @@ void aicwf_usb_host_txdesc_push(struct usb_host_env_tag *env, const int queue_id
 void aicwf_usb_host_tx_cfm_handler(struct usb_host_env_tag *env, u32 *data)
 {
     u32 queue_idx  = 0;//data[0];
-    //struct rwnx_hw *rwnx_hw = (struct rwnx_hw *)env->pthis;
     struct sk_buff *skb = NULL;
     struct rwnx_txhdr *txhdr;
-	AICWFDBG(LOGTRACE, "%s Enter \n", __func__);
-    //printk("sdio_host_tx_cfm_handler, %d, 0x%08x\r\n", queue_idx, data[1]);
-    // TX confirmation descriptors have been received
-   // REG_SW_SET_PROFILING(env->pthis, SW_PROF_IRQ_E2A_TXCFM);
-    //while (1)
+    uint32_t used_idx;
+    unsigned long host_id;
+
+    if (!env) {
+        printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] ERROR: env is NULL!\n");
+        return;
+    }
+    if (!data) {
+        printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] ERROR: data is NULL!\n");
+        return;
+    }
+
+    printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] entered: env=%p, data[0]=0x%x, data[1]=%u\n",
+           env, data[0], data[1]);
+
+    used_idx = data[1];
+    host_id = env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT];
+
+    printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] used_idx=%u, host_id=%p\n", used_idx, (void *)host_id);
+
+    // Reset the host id in the array
+    env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT] = 0;
+
+    if (host_id == 0)
     {
-        // Get the used index and increase it. We do the increase before knowing if the
-        // current buffer is confirmed because the callback function may call the
-        // ipc_host_txdesc_get() in case flow control was enabled and the index has to be
-        // already at the good value to ensure that the test of FIFO full is correct
-        //uint32_t used_idx = env->txdesc_used_idx[queue_idx]++;
-		uint32_t used_idx = data[1];
-        //uint64_t host_id = env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT];
-        unsigned long host_id = env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT];
-
-        // Reset the host id in the array
-        env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT] = 0;
-
-        // call the external function to indicate that a TX packet is freed
-        if (host_id == 0)
-        {
-            // No more confirmations, so put back the used index at its initial value
-            env->txdesc_used_idx[queue_idx] = used_idx;
-			AICWFDBG(LOGERROR, "ERROR:No more confirmations\r\n");
-            return;
-            //break;
-        }
-		AICWFDBG(LOGINFO, "usb_host_tx_cfm_handler:used_idx=%d, hostid=%p, 0x%p, status=%x\r\n",used_idx, (void *)host_id, env->pthis, data[0]);
-
-        // set the cfm status
-        skb = (struct sk_buff *)host_id;
-        txhdr = (struct rwnx_txhdr *)skb->data;
-        txhdr->hw_hdr.cfm.status = (union rwnx_hw_txstatus)data[0];
-        //txhdr->hw_hdr.status = data[1];
-        //if (env->cb.send_data_cfm(env->pthis, host_id) != 0)
-        if (rwnx_txdatacfm(env->pthis, (void *)host_id) != 0)
-        {
-            // No more confirmations, so put back the used index at its initial value
-            env->txdesc_used_idx[queue_idx] = used_idx;
-            env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT] = host_id;
-            // and exit the loop
-            AICWFDBG(LOGERROR, "ERROR:rwnx_txdatacfm,\r\n");
-          //  break;
-        }
+        env->txdesc_used_idx[queue_idx] = used_idx;
+        printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] ERROR: host_id is 0 (No more confirmations)!\n");
+        return;
+    }
 
+    skb = (struct sk_buff *)host_id;
+    if (!skb || !skb->data) {
+        printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] ERROR: skb or skb->data is NULL for host_id=%p!\n", (void *)host_id);
+        return;
     }
+
+    txhdr = (struct rwnx_txhdr *)skb->data;
+    txhdr->hw_hdr.cfm.status = (union rwnx_hw_txstatus)data[0];
+
+    printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] calling rwnx_txdatacfm pthis=%p, host_id=%p, status=0x%x\n",
+           env->pthis, (void *)host_id, data[0]);
+
+    if (rwnx_txdatacfm(env->pthis, (void *)host_id) != 0)
+    {
+        env->txdesc_used_idx[queue_idx] = used_idx;
+        env->tx_host_id[queue_idx][used_idx % USB_TXDESC_CNT] = host_id;
+        printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] rwnx_txdatacfm returned non-zero!\n");
+    }
+
+    printk(KERN_ERR "MONDBG: [host_tx_cfm_handler] exit\n");
 }
 
 int aicwf_rwnx_usb_platform_init(struct aic_usb_dev *usbdev)
